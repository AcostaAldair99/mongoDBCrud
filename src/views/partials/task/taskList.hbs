<table>
    <thead>
        <tr>
            <th>No.#</th>
            <th>Title</th>
            <th>Description</th>
            <th>Operations</th>
        </tr>
    </thead>
    <tbody>
        {{#each tasks}}
        <tr>
            <td>{{@index}}</td>
            <td>{{title}}</td>
            <td>{{description}}</td>
            <td><a href="/edit/{{_id}}">Edit</a>
            <a href="/delete/{{_id}}">Delete</ag>
            {{# if done}}
                <a href="/toogleDone/{{_id}}">Undone</a>
            {{else}}
                <a href="/toogleDone/{{_id}}">Done</a>
            {{/if}}
            </td>
        </tr>
        {{/each}}
    </tbody>
</table>
